# ACTES 1-3 — cartes de terrain


================================================================================
## Génération d'univers Exilium : carte 100% virtuelle-lazy, aucune table galaxie/système, reset partiel existant (compact-universe.sql jamais appliqué)

- FAIT : Il n'existe AUCUNE table galaxie/système/position : l'univers n'est jamais généré au seed. Ses dimensions sont 3 clés jsonb de universe_config (galaxies=9, systems=499, positions=16 en prod) et tout le contenu d'une coordonnée vide est régénéré à la volée, déterministiquement.
  PREUVE : packages/db/src/schema/ (listing : aucun schema galaxy/system) ; SELECT key,value FROM universe_config → galaxies=9, systems=499, positions=16 ; apps/api/src/modules/galaxy/galaxy.service.ts:154-200 (slots vides calculés à chaque requête)
- FAIT : La génération procédurale est un PRNG seedé (mulberry32) dont le seed est UNIQUEMENT fonction des coordonnées : seed = galaxy*1_000_000 + system*1_000 + position. Conséquence directe : une « carte neuve » aux mêmes dimensions serait IDENTIQUE à l'actuelle — il n'y a pas de world-seed d'univers à salter.
  PREUVE : packages/game-engine/src/formulas/biomes.ts:34-49 (seededRandom + coordinateSeed)
- FAIT : Chaîne de génération d'une position vide : température déterministe par position (maxTemp = 40 + (8-position)*30, offset 0 pour stabilité), puis type de planète tiré par brackets de température (typeRng = seed XOR 0x9E3779B9), puis nombre de biomes (1-5) et biomes pondérés par rareté avec le seed nu. Même logique dupliquée dans galaxy.service, colonize.handler et explore.handler — cohérence garantie par le seed partagé.
  PREUVE : packages/game-engine/src/formulas/planet.ts:6-8 ; planet-type.ts:8-33 ; galaxy.service.ts:160-173 ; colonize.handler.ts:110-123 ; explore.handler.ts:101-107
- FAIT : Attribution de la planète de départ : register (auth.router) enveloppe user + homeworld dans UNE transaction ; createHomePlanet → findFreeHomeworldCoordinate ancre le spawn sur le DERNIER homeworld créé (clustering des nouveaux), garde sa galaxie, tire un système au hasard dans un rayon élargissant (spawn_radius, défaut 10 — la clé n'existe pas en base), et une position libre dans [home_planet_position_min=4, home_planet_position_max=12]. Diamètre fixe homePlanetDiameter=12000, classe 'homeworld' (planet_types.role), tous bâtiments niveau 0, retry ×3 sur collision unique_coordinates.
  PREUVE : apps/api/src/modules/auth/auth.router.ts:44-56 ; apps/api/src/modules/planet/planet.service.ts:42-119 et 219-305 ; grep spawn_radius → une seule occurrence (jamais seedée)
- FAIT : Les ceintures d'astéroïdes sont créées PARESSEUSEMENT : la position vient de universe_config.belt_positions=[8,16] (consommée par mine/explore/colonize handlers, galaxy.service, pve.service), mais la ligne asteroid_belts + 3-5 asteroid_deposits n'est insérée qu'au premier ciblage via getOrCreateBelt. Le contenu des gisements est NON déterministe (Math.random) et les probabilités/quantités sont des constantes hardcodées littéralement typées `8 | 16` — changer les positions de ceintures exige de modifier ce service.
  PREUVE : packages/db/src/game-config-data.ts (clé belt_positions:[8,16]) ; apps/api/src/modules/pve/asteroid-belt.service.ts:5-22 (constantes PRESENCE_PROBABILITY/QUANTITY_RANGE keyées 8|16) et 63-91 (getOrCreateBelt)
- FAIT : Les biomes ne sont persistés que sur événement : discovered_biomes/discovered_positions à l'exploration (mémoire par joueur), planet_biomes à la colonisation (avec flag active selon découverte préalable). L'affichage galaxie lit les découvertes persistées, pas la régénération — immunité aux changements de catalogue.
  PREUVE : packages/db/src/schema/biomes.ts:16-43 ; explore.handler.ts:77-120 ; colonize.handler.ts:148-214 ; galaxy.service.ts:15-19 (commentaire « authoritative source »)
- FAIT : Seul code de « reset d'univers » existant : packages/db/src/scripts/compact-universe.sql — relocalisation complète vers 1 galaxie / 50 systèmes (1 système par joueur, max 50) : refund des flottes actives, remap planets/debris/pve_missions/mission_reports, DELETE des asteroid_belts hors bornes (« regénérées au premier accès »), UPDATE universe_config galaxies=1/systems=50. Il n'a JAMAIS été appliqué en prod : la base montre encore 9/499 et 91 planètes éparpillées sur les galaxies 1,2,4,5,6,7 (25 users, 242 belts, 3082 deposits).
  PREUVE : packages/db/src/scripts/compact-universe.sql:281-284, 351-353 ; SQL : SELECT galaxy, COUNT(*) FROM planets GROUP BY galaxy → 6 galaxies occupées ; SELECT value FROM universe_config WHERE key='systems' → 499
- FAIT : PIÈGE au redimensionnement n°1 : le seed upsert TOUTES les clés universe_config (onConflictDoUpdate) depuis UNIVERSE_CONFIG dans game-config-data.ts — un `db:seed` post-déploiement écraserait une carte compactée en remettant galaxies=9/systems=499. Toute nouvelle dimension doit être changée dans le code seed, pas seulement en base.
  PREUVE : packages/db/src/seed-game-config.ts:606-608 ; packages/db/src/game-config-data.ts (UNIVERSE_CONFIG : galaxies:9, systems:499)
- FAIT : PIÈGE n°2 : la topologie en anneau (wraparound) du calcul de distance utilise maxGalaxies=9/maxSystems=499 HARDCODÉS dans le moteur, et buildFleetConfig ne câble jamais ces deux champs depuis universe_config — après redimensionnement à 50 systèmes, le wrap serait calculé sur 499 et l'anneau serait cassé (jamais plus court).
  PREUVE : packages/game-engine/src/formulas/fleet.ts:59 (`config.maxGalaxies ?? 9`) et 65 (`?? 499`) ; apps/api/src/modules/fleet/fleet.helpers.ts:8-18 (buildFleetConfig sans maxGalaxies/maxSystems)
- FAIT : Carte neuve = vider les tables d'état-carte : planets (cascade planet_buildings/planet_ships/planet_defenses/planet_biomes), asteroid_belts (cascade asteroid_deposits), debris_fields, discovered_positions, discovered_biomes, fleet_events, pve_missions (coords en jsonb parameters), mission_reports (coordinates jsonb), exploration_reports, mission_center_state. Les définitions (planet_types, biome_definitions, universe_config) sont du config-as-data reseedable et n'ont pas à bouger.
  PREUVE : packages/db/src/schema/planets.ts:64 (unique_coordinates), asteroid-belts.ts, debris-fields.ts, biomes.ts:24-43 ; mission_reports.coordinates jsonb (schema/mission-reports.ts:15) ; compact-universe.sql sections 5-9 (liste exhaustive des tables à coords)
- FAIT : Les clés universe_config qui dimensionnent la carte : galaxies, systems, positions, belt_positions, home_planet_position_min/max (4-12), homePlanetDiameter (12000), spawn_radius (absente, fallback 10), name, speed ; plus les fleet_distance_* (galaxy_factor 20000, system_base 2700, system_factor 95…) qui règlent le coût du déplacement à l'échelle de la carte. L'admin peut déjà éditer n'importe quelle clé à chaud (upsert + invalidation du cache par NOTIFY pg vers tous les workers PM2).
  PREUVE : packages/db/src/game-config-data.ts:164-224 ; apps/api/src/modules/admin/game-config.service.ts:511-515 (updateUniverseConfig) et 32-66 (INVALIDATE_CHANNEL) ; apps/web/src/components/common/CoordinateInput.tsx:21-22 (l'UI suit universe_config)
- FAIT : Aucune entité NPC persistée sur la carte aujourd'hui : les pirates sont des pve_missions éphémères par joueur, position tirée au hasard HORS ceintures, résolues contre pirate_templates — les « systèmes des Premiers » (capitaine par système, ports/prises/silos) n'ont aucun support existant et nécessiteraient une première matérialisation des systèmes en table.
  PREUVE : apps/api/src/modules/pve/pve.service.ts:380-386 (position aléatoire excluant beltSet) ; schema/pve-missions.ts (missions par user, coords en jsonb)
FICHIERS CLÉS : /opt/exilium/packages/db/src/schema/planets.ts, /opt/exilium/packages/db/src/schema/asteroid-belts.ts, /opt/exilium/packages/db/src/schema/biomes.ts, /opt/exilium/packages/db/src/schema/debris-fields.ts, /opt/exilium/packages/db/src/schema/game-config.ts, /opt/exilium/packages/db/src/game-config-data.ts, /opt/exilium/packages/db/src/seed-game-config.ts, /opt/exilium/packages/db/src/scripts/compact-universe.sql, /opt/exilium/packages/game-engine/src/formulas/biomes.ts, /opt/exilium/packages/game-engine/src/formulas/planet-type.ts, /opt/exilium/packages/game-engine/src/formulas/planet.ts, /opt/exilium/packages/game-engine/src/formulas/fleet.ts, /opt/exilium/apps/api/src/modules/planet/planet.service.ts, /opt/exilium/apps/api/src/modules/auth/auth.router.ts, /opt/exilium/apps/api/src/modules/galaxy/galaxy.service.ts, /opt/exilium/apps/api/src/modules/pve/asteroid-belt.service.ts, /opt/exilium/apps/api/src/modules/pve/pve.service.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/colonize.handler.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/explore.handler.ts, /opt/exilium/apps/api/src/modules/fleet/fleet.helpers.ts, /opt/exilium/apps/api/src/modules/admin/game-config.service.ts
QUESTIONS : Faut-il introduire un « world seed » en universe_config injecté dans coordinateSeed ? Sans lui, une carte neuve aux mêmes dimensions reproduit exactement les mêmes types de planètes et biomes à chaque coordonnée (le seed est un pur produit de g:s:p). | Reset complet ou relocalisation ? compact-universe.sql préserve les empires (comptes + planètes déplacées) alors que le brief « carte neuve, tout le monde à égalité » suggère plutôt un TRUNCATE de l'état-carte ET de l'état-joueur (recherches, empire_progression, flagships, exilium…) en gardant les comptes — à trancher avec Julien. | Quelles belt_positions pour la nouvelle carte ? Le service de ceintures est typé littéralement `8 | 16` avec des tables de probabilités hardcodées par position — toute nouvelle disposition (ex. ceintures dans les systèmes des Premiers) demande une refonte de asteroid-belt.service.ts, pas juste un changement de clé. | Le bug maxGalaxies/maxSystems non câblés dans buildFleetConfig doit-il être corrigé avant le redimensionnement (topologie anneau cassée hors 9/499), ou la nouvelle carte abandonne-t-elle l'anneau (bord de carte = fiction des exilés posés « au bord ») ? La fiction plaide pour une carte à bords, ce qui inverserait le sens du fix. | Les systèmes des Premiers imposent une première table « systems » persistée (capitaine, type de secteurs ports/prises/silos, état de tenue). Faut-il matérialiser TOUS les systèmes au seed de la nouvelle carte (fini le lazy intégral) ou seulement les systèmes tenus par les Premiers, le reste restant virtuel ? | Le clustering du spawn (ancrage sur le dernier homeworld + rayon 10) place les nouveaux au même endroit — compatible avec « exilés posés au bord de la carte », mais le dimensionnement sur N joueurs (min 4-12 par système, 9 positions habitables hors ceintures) doit décider : un système par joueur (comme compact-universe) ou spawn groupé multi-joueurs par système ? | planet_types.positions (jsonb [1,2,3]…) semble hérité : le picker réel est pondéré par température, pas par ce champ — confirmer qu'il est mort avant de s'appuyer dessus pour la nouvelle carte.

================================================================================
## Moteur de combat Exilium : mécanique saine et extensible, PvE trivial par budget de puissance décorrélé — diagnostic et points d'extension pour des missions à enjeu

- FAIT : Déroulé d'un combat : boucle de rounds (max 6 en prod) à tirs SIMULTANÉS — chaque camp tire sur des clones de l'état de début de round, les dégâts sont appliqués ensuite, puis les boucliers de TOUS les survivants régénèrent à 100 % chaque round ; le bouclier planétaire est une unité spéciale ressuscitée chaque round.
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/combat.ts:586-668 (boucle), 597-599 (clones), 633-635 (regen bouclier), 637-644 (revive bouclier planétaire) ; universe_config: combat_max_rounds=6
- FAIT : fireSalvo : chaque unité a des batteries d'armes (WeaponProfile) avec catégorie-cible préférée ; ciblage = catégorie prioritaire d'abord, sinon fallback par targetOrder croissant, cible aléatoire dans la catégorie. Traits : rafale (tirs bonus si la 1re cible matche la catégorie, ex. croiseur +6 tirs anti-light) et chainKill (1 tir bonus sur kill, non chaînable, ex. intercepteur).
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/combat.ts:409-458 (fireSalvo), 239-268 (selectTarget) ; profils en base : SELECT weapon_profiles FROM ship_definitions
- FAIT : fireShot : le bouclier absorbe d'abord (consommé sur le tir), le surplus est réduit par l'armure (réduction plate, min 1 dégât garanti par tir atteignant la coque), le reste va à la coque. shield_pierce (fork Armement S1) = fraction ADDITIVE de l'absorption bouclier ennemie ignorée, côté attaquant uniquement, clampée [0, 0.999].
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/combat.ts:308-407 (fireShot), 330-331 (pierce), 370 (armure + minDamage), 538 (clamp)
- FAIT : 7 catégories de coques : light/medium/heavy/shield/defense/capital/support par targetOrder ; capital (vaisseau amiral) non-targetable mais ciblé avant support. Issue = tout-ou-rien : victoire attaquant uniquement si TOUS les défenseurs sont détruits en ≤ maxRounds, sinon draw (les deux camps vivants) ; débris = 35 % du coût des vaisseaux détruits, réparation défenses 70 %.
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/combat-config.ts:3-15 ; combat.ts:682-694 ; universe_config: combat_debris_ratio=0.35
- FAIT : PvE trivial CONFIRMÉ en prod : 1128/1138 victoires (99,1 %), 1,27 round de moyenne, 954 combats réglés en 1 round, 948/1138 (83 %) à zéro perte. Les seules pertes notables : quelques intercepteurs, et 26 flagships (catégorie medium en base, donc ciblable normalement).
  PREUVE : SELECT count(*), sum(CASE WHEN result->>'outcome'='attacker' THEN 1 END), avg((result->>'roundCount')::numeric) FROM mission_reports WHERE mission_type='pirate' → 1138 | 1128 | 1.27 ; count FILTER (WHERE result->'attackerLosses'='{}') → 948
- FAIT : Cause n°1 de la trivialité : le budget de puissance pirate est DÉCORRÉLÉ de la flotte envoyée. FP pirate = (tier_min..tier_max) × niveau du centre de missions (easy 2-5, medium 5-12, hard 15-30), avec un cap qui ne joue QUE vers le bas (0,8 × FP total du joueur). Résultat mesuré : ratio FP attaquant/défenseur médian = 20×, moyen = 63×, 337 combats sur 1101 au-delà de 50×.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pirate.service.ts:38-61 (buildScaledPirateFleet) ; universe_config pirate_fp_* ; SQL: percentile_cont(0.5) sur (attackerFP/defenderFP) FROM mission_reports → 20.5
- FAIT : Cause n°2 : les templates pirates ne sont PAS des flottes absolues mais des ratios re-scalés au budget (scaleFleetToFP) — « Armada pirate » (3 croiseurs + 4 frégates + 2 cuirassés) se matérialise en 4-7 intercepteurs quand le budget est petit. Toutes les missions récentes en base = {"interceptor": 4-7}. FP unitaires : intercepteur 2, frégate 13, croiseur 67, cuirassé 166 → une mission easy niveau 3 = 6-15 FP.
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/fp.ts:161-214 (scaleFleetToFP) ; SELECT parameters->>'scaledFleet' FROM pve_missions WHERE mission_type='pirate' ORDER BY created_at DESC → uniquement des intercepteurs
- FAIT : Cause n°3 : les pirates combattent à multiplicateurs 1/1/1 (zéro recherche) avec les MÊMES définitions de vaisseaux que les joueurs (aucune unité pirate dédiée), sans défenses ni bouclier planétaire (defenderDefenses: {}), pendant que le joueur bénéficie de +10 %/niveau de recherche armes/bouclier/blindage (un joueur est à weapons 13 → ×2,3) et du shield_pierce.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pirate.service.ts:73-77 (multiplicateurs 1), 137-144 (defenderDefenses vide) ; SELECT ships FROM pirate_templates (ids = vaisseaux joueurs) ; SELECT research_id, level FROM user_research_levels
- FAIT : Cause n°4 : aucun enjeu. Le butin est écrasé par pve_loot_multiplier=0.1 appliqué à la génération (commit 34641121 « nerf pirate mission resource rewards to 10% ») : missions easy récentes = 240-1400 minerai alors qu'UN intercepteur coûte 2250 minerai + 750 silicium. En cas de défaite la mission est simplement remise en 'available'. Les missions sont donc farmées pour les hooks annexes (XP d'empire, drop d'exilium, quêtes journalières), pas pour le combat.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pve.service.ts:427-435 ; pirate.handler.ts:112-116 (releaseMission), 255-277 (hooks XP/exilium) ; git log 34641121 ; SELECT rewards FROM pve_missions ORDER BY created_at DESC
- FAIT : Le chemin de combat PvE DIVERGE du chemin PvP : pirate.service recode en dur une liste de 4 catégories (light/medium/heavy/support — sans shield/defense/capital) et son propre shipCombatConfigs SANS les weaponProfiles (batteries multi-cibles, rafale, chainKill ignorés en PvE !), alors que le PvP (attack.handler) passe par buildShipCombatConfigs + COMBAT_CATEGORIES complètes + défenses + bouclier planétaire.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pirate.service.ts:80-102 (configs sans weapons:profiles), 117-122 (4 catégories hardcodées) vs /opt/exilium/apps/api/src/modules/fleet/fleet.types.ts:168-198 et attack.handler.ts:243-285
- FAIT : À GARDER : le moteur pur est sain et déjà extensible pour « les Premiers » — 100 % config-driven (CombatInput accepte n'importe quels fleets/defenses/multipliers/configs d'unités), RNG seedé pour replay déterministe, detailedLog événement-par-événement pour les replays UI, 50 tests dont 12 scénarios verrouillés en snapshot. Ajouter des unités Premiers = nouvelles lignes ship_definitions avec weapon_profiles propres, zéro changement moteur ; le bouclier planétaire montre le pattern « unité spéciale injectée » réutilisable pour des mécaniques de secteur (station de silo, batterie de port).
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/combat.ts:73-87 (CombatInput), 270-280 (rng seedé), 546-559 (injection unité spéciale) ; combat.scenarios.test.ts:6-12 (snapshots verrouillés)
- FAIT : Points d'extension propres pour la refonte : (a) registry des handlers de mission fleet.service.ts:75-88 — un « assaut de secteur » = nouvelle classe MissionHandler ; (b) PhasedMissionHandler (utilisé par mine) supporte les missions multi-phases → sièges/occupations de ports ; (c) mission_reports stocke déjà les rounds complets + FP des deux camps ; (d) le simulateur bac-à-sable (combat.service.simulate, 200 runs agrégés winRate/pertes moyennes) est l'outil de préparation d'assaut déjà en place ; (e) la formule FP (fp.ts) reste la règle à mesurer pour composer des garnisons fixes.
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/fleet.service.ts:75-88 ; fleet.types.ts:144-150 (PhasedMissionHandler) ; combat/combat.service.ts:189-274 (simulate)
- FAIT : À REFAIRE pour « le combat porte des missions à enjeu » : (1) génération du contenu PvE — remplacer budget FP scalé (tier × centerLevel + cap 0.8) par des garnisons fixes par secteur des Premiers, avec vraies compositions lourdes ; (2) puissance des Premiers — multiplicateurs de recherche par système/capitaine au lieu de 1/1/1, et unités dédiées avec profils d'armes ; (3) router le PvE sur le chemin PvP complet (7 catégories, weaponProfiles, défenses, bouclier planétaire) pour que ports/prises/silos soient des positions défendues ; (4) couche récompense — sortir du butin-cargo ×0,1 vers les récompenses territoriales/qualitatives (flux d'exilium, nœuds de recherche, vaisseaux spéciaux) ; (5) modèle de risque — la défaite doit coûter (pertes réelles sur garnisons calibrées, pas re-roll gratuit) ; le tout-ou-rien wipe-en-6-rounds peut rester si les garnisons imposent plusieurs assauts avec état persistant (pattern asteroid_deposits : état entamé mission après mission).
  PREUVE : Synthèse des constats précédents ; pattern état persistant : /opt/exilium/apps/api/src/modules/pve/pve.service.ts:39-56 (deposits à quantités restantes)
- FAIT : Comparaison stats pirate_templates vs joueurs : identité parfaite — les 10 templates référencent exclusivement interceptor/frigate/cruiser/battlecruiser/smallCargo, c'est-à-dire les définitions joueurs (intercepteur 4dmg×3 tirs/6 bouclier/12 coque/1 armure ; cuirassé 50+2×10dmg/40/120/6). Le « pirate » n'existe pas comme entité de combat : c'est un joueur sans recherche, sans défense, sans profils d'armes, et en sous-nombre 20×. Ce n'est ni la formule ni les templates seuls — c'est l'assemblage budget+multiplicateurs+récompenses qui rend le PvE trivial.
  PREUVE : SELECT ships FROM pirate_templates ; SELECT id, weapons, shot_count, shield, hull, base_armor, weapon_profiles FROM ship_definitions ORDER BY sort_order
FICHIERS CLÉS : /opt/exilium/packages/game-engine/src/formulas/combat.ts, /opt/exilium/packages/game-engine/src/formulas/combat-config.ts, /opt/exilium/packages/game-engine/src/formulas/fp.ts, /opt/exilium/packages/game-engine/src/formulas/combat.scenarios.test.ts, /opt/exilium/apps/api/src/modules/pve/pirate.service.ts, /opt/exilium/apps/api/src/modules/pve/pve.service.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/pirate.handler.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/attack.handler.ts, /opt/exilium/apps/api/src/modules/fleet/combat.helpers.ts, /opt/exilium/apps/api/src/modules/fleet/fleet.types.ts, /opt/exilium/apps/api/src/modules/fleet/fleet.service.ts, /opt/exilium/apps/api/src/modules/combat/combat.service.ts
QUESTIONS : Les secteurs des Premiers (ports/prises/silos) sont-ils des combats de POSITION (défenses + bouclier planétaire + flotte, chemin attack.handler) ou des flottes en espace ouvert (chemin pirate actuel) ? Le moteur supporte les deux sans modification ; la fiction « tiennent des systèmes entiers » pousse vers la position défendue. | Unités Premiers dédiées (nouvelles ship_definitions avec profils d'armes et contre-jeu propres, ex. anti-capital pour menacer le flagship) ou réutilisation des vaisseaux joueurs avec multiplicateurs de capitaine ? Aujourd'hui : réutilisation pure sans profils. | Le tout-ou-rien (victoire = wipe complet en ≤6 rounds, sinon draw) convient-il aux sièges ? Alternative : garnison à état persistant entamée assaut après assaut (pattern asteroid_deposits déjà en prod) — quel modèle pour un port qui « tient » face à plusieurs amis en coop ? | Comment empêcher le sur-nombre de rester LA stratégie dominante une fois les garnisons fixées ? (le cap 0.8×FP joueur disparaît ; candidats : coût hydrogène du convoyage, slots d'escadre par mission, maxRounds comme limite de temps d'assaut, tir simultané qui garantit des pertes contre grosses garnisons) | Calibrage des multiplicateurs Premiers : scalés par richesse de zone (bord pauvre → centre riche) et par capitaine nommé ? Aucune donnée d'équilibrage n'existe pour des combats à ratio FP ~1:1 avec le moteur actuel — les snapshots ne couvrent que des petits effectifs ; game-sim (Phase 2b backlog) serait l'outil pour calibrer avant mise en prod. | Le simulateur bac-à-sable (combat.service.simulate) doit-il devenir l'outil de préparation d'assaut officiel côté UI (projection de pertes contre une garnison connue via espionnage) ? Il ignore aujourd'hui le shield_pierce défenseur et le flagship attaquant.

================================================================================
## Centre de missions PVE — cycle de vie, génération des récompenses, modulation par niveau, et écarts avec l'ambition « cœur du jeu »

- FAIT : La découverte de missions est entièrement paresseuse : materializeDiscoveries n'est appelé que dans la query pve.getMissions (ouverture de la page Missions ou Galaxy) ; aucun cron serveur ne génère de missions.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pve.router.ts:15 (seul appelant) ; /opt/exilium/apps/api/src/workers/worker.ts:97-140 (aucun cron pve hors deposit-regen)
- FAIT : L'état de découverte est par joueur : mission_center_state a user_id comme clé primaire, avec deux timers (next_discovery_at pour le minage, next_pirate_discovery_at pour les pirates, décalés de 50% du cooldown) ; 13 joueurs ont un état.
  PREUVE : /opt/exilium/packages/db/src/schema/mission-center-state.ts:4 ; pve.service.ts:117-118 ; SQL: SELECT count(*) FROM mission_center_state → 13
- FAIT : Cooldown de découverte = max(1, 7 − centerLevel) heures, partagé entre minage et pirates ; rattrapage en cas d'absence : n intervalles manqués créent jusqu'au cap de missions d'un coup.
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/pve.ts:31-36 ; /opt/exilium/apps/api/src/modules/pve/pve.service.ts:178-206
- FAIT : Caps concurrents : 3 missions de minage (pve_max_concurrent_missions=3 en base) et 2 pirates (pve_max_pirate_missions absent de universe_config → défaut code 2), avec re-vérification anti-race avant insert (cluster PM2).
  PREUVE : pve.service.ts:166-167, 227-234, 355-366 ; SQL: SELECT key FROM universe_config WHERE key LIKE 'pve%' (pas de pve_max_pirate_missions)
- FAIT : Le « bâtiment » centre de missions n'existe plus : il a été retiré (commit c7f43201 « retirer les bâtiments Centre/Relais de missions ») ; le niveau effectif = mission_default_level (3) + floor((niveauEmpire−1)/5). Le front garde seulement l'illustration du bâtiment en bannière.
  PREUVE : pve.service.ts:75-84 ; /opt/exilium/packages/game-engine/src/formulas/empire-level.ts:78-84 ; git log apps/api/src/modules/pve → c7f43201, b6b62b47 ; Missions.tsx:157 (illustration 'missionCenter')
- FAIT : Le niveau de missions module : la cadence (7−lvl h, min 1h), la taille des gisements (15000 + 5000×(lvl−1), variance ×0.6–1.6), l'accès à la 2e ceinture (position 16 dès lvl 3), le déblocage des tiers pirates (medium ≥4, hard ≥6) et le FP pirate (cible = U(min,max)×centerLevel).
  PREUVE : pve.service.ts:216, 253, 368-374 ; pirate.service.ts:47-55 ; formulas/pve.ts:42-48 ; SQL universe_config: pve_tier_medium_unlock=4, pve_tier_hard_unlock=6
- FAIT : Niveaux actuels en prod : top joueurs empire 19 → niveau missions 6 (cooldown 1h, tier hard) ; joueurs empire 5 → niveau 3 (4h, easy seulement).
  PREUVE : SQL: SELECT username, level, 3+floor((level-1)/5) FROM empire_progression JOIN users → Flow/JMFion 19→6, Roukinax 5→3
- FAIT : Cycle de vie : available → (pirate uniquement) in_progress au départ de flotte → completed (victoire pirate / gisement vidé) ou retour à available (défaite/nul pirate) ; les missions mine restent 'available' pour permettre plusieurs flottes jusqu'à épuisement du gisement.
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts:293-297 ; pirate.handler.ts:112-116 ; mine.handler.ts:227-229
- FAIT : L'expiration 7 jours est du code mort : expireOldMissions (DELETE des available > pve_mission_expiry_days) n'est branché sur aucun cron ; preuve en base : 55 missions 'available' de plus de 7 jours, la plus vieille du 2026-03-25.
  PREUVE : pve.service.ts:452-460 ; grep expireOldMissions → aucun appelant ; SQL: count(*) WHERE status='available' AND created_at < now()-'7 days' → 55
- FAIT : Le dismiss passe la mission en status 'expired' (refusé si une flotte est en vol) ; le « cooldown 24h » annoncé dans l'aide du front (pve_dismiss_cooldown_hours=24 en config) n'est implémenté nulle part.
  PREUVE : pve.service.ts:302-330 ; Missions.tsx:573 (texte d'aide) ; grep pve_dismiss_cooldown → seule occurrence = seed game-config-data.ts:197
- FAIT : Récompenses minage : gisement généré à la découverte (taille × niveau, composition ~60/30/10 avec offsets aléatoires, hydrogène cappé à 1500 et redistribué, talent pve_loot appliqué à l'affichage) ; l'extraction réelle se fait par voyages en phases aller→prospection→extraction→retour, limitée par min(extraction flotte, soute×(1−scories)), scories 0.5 réduites par la recherche raffinage (×0.85^niveau).
  PREUVE : pve.service.ts:244-299 ; mine.handler.ts:24-229 ; formulas/pve.ts:78-162
- FAIT : Les gisements sont des objets-monde PARTAGÉS entre joueurs : asteroid_belts uniques par coordonnées, asteroid_deposits globaux avec extraction atomique (FOR UPDATE) et régénération 4–8h après épuisement — un substrat multi-joueurs existe déjà côté minage ; getMissions réécrit d'ailleurs rewards avec le restant réel du gisement.
  PREUVE : /opt/exilium/apps/api/src/modules/pve/asteroid-belt.service.ts:63-91, 137-177, 179-209 ; pve.service.ts:39-56
- FAIT : Récompenses pirates figées à la génération : template tiré au hasard dans pirate_templates (10 gabarits, 3 tiers), flotte scalée au FP cible (cap 0.8× le FP total du joueur toutes planètes), butin = rewards du template × (FP réel/FP template) × pve_loot_multiplier 0.1 × (1+talent) ; vaisseaux bonus tirés à la victoire (chance 0.2–0.3, uniquement des coques standard interceptor/cruiser/battlecruiser).
  PREUVE : pve.service.ts:352-450 ; pirate.service.ts:38-61, 157-178 ; SQL: SELECT * FROM pirate_templates → 10 lignes
- FAIT : La victoire pirate alimente déjà les méta-boucles : 15 XP d'empire (empire_xp_pve_victory), tentative de drop d'exilium (source 'pve'), hook quêtes quotidiennes, champ de débris, et rapport structuré (mission_reports lié à pve_mission_id).
  PREUVE : pirate.handler.ts:254-277 ; game-config-data.ts:237 ; /opt/exilium/packages/db/src/schema/mission-reports.ts
- FAIT : enemyShips est mis à null côté liste (pve.getMissions n'expose que pirateFP), MAIS getMissionById renvoie la ligne brute avec parameters.scaledFleet : la composition exacte de la flotte pirate fuit déjà au client sur l'écran d'envoi de flotte (PveMissionBanner) — c'est un choix d'affichage, pas une donnée manquante.
  PREUVE : pve.router.ts:21-23 vs pve.service.ts:61-69 ; /opt/exilium/apps/web/src/components/fleet/PveMissionBanner.tsx:18-25 (lit params.pirateFP, ignore scaledFleet)
- FAIT : Seules les missions de minage sont visibles sur la carte (CTA dans la vue ceinture du système) ; les repaires pirates sont des instances fantômes par joueur, posées sur une position aléatoire hors ceinture mais invisibles dans la galaxie.
  PREUVE : /opt/exilium/apps/web/src/components/galaxy/GalaxySystemView/DetailPanel/ModeBelt.tsx:25-53 ; pve.service.ts:379-386 ; grep pirate dans apps/web/src/pages/Galaxy.tsx → rien
- FAIT : Les tiers n'existent que pour les pirates (difficulty_tier easy/medium/hard, tirage uniforme parmi les tiers débloqués) ; le minage n'a aucun tier ; les lignes mission_type='exploration' en base sont des résidus d'un système supprimé (dernière le 2026-05-11, cleanup commit 8e1fa106).
  PREUVE : pve-missions.ts (schéma) ; pve.service.ts:370-374 ; SQL: max(created_at) WHERE mission_type='exploration' → 2026-05-11 ; git log → 8e1fa106
- FAIT : Le routage des types de missions est déjà data-driven : mission_definitions porte requires_pve_mission (mine, pirate, abandon_return), required_ship_roles et le handler par mission est enfichable (PhasedMissionHandler avec phases nommées) — ajouter des types de missions est structurellement prévu.
  PREUVE : SQL: SELECT id, requires_pve_mission FROM mission_definitions ; /opt/exilium/packages/db/src/schema/mission-definitions.ts ; mine.handler.ts:9 (PhasedMissionHandler)
- FAIT : Le tutoriel (quête 15) injecte une première mission de minage au niveau 1 via generateDiscoveredMission — précédent existant d'alimentation scriptée hors cooldown.
  PREUVE : /opt/exilium/apps/api/src/modules/tutorial/tutorial.service.ts:412-415
- FAIT : Volume vécu : ~2258 missions mine et ~1134 pirates complétées, 3729 fleet_events liés à une mission PVE — la boucle est déjà la plus jouée du jeu ; « missions de groupe » exigerait de casser l'hypothèse user_id NOT NULL de pve_missions (ou une table world-anchored séparée) plus une découverte poussée par le serveur au lieu du lazy par joueur.
  PREUVE : SQL: SELECT mission_type, status, count(*) FROM pve_missions GROUP BY 1,2 ; schéma pve-missions.ts (userId notNull)
FICHIERS CLÉS : /opt/exilium/apps/api/src/modules/pve/pve.service.ts, /opt/exilium/apps/api/src/modules/pve/pve.router.ts, /opt/exilium/apps/api/src/modules/pve/pirate.service.ts, /opt/exilium/apps/api/src/modules/pve/asteroid-belt.service.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/mine.handler.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/pirate.handler.ts, /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts, /opt/exilium/apps/api/src/workers/worker.ts, /opt/exilium/packages/game-engine/src/formulas/pve.ts, /opt/exilium/packages/game-engine/src/formulas/empire-level.ts, /opt/exilium/packages/db/src/schema/pve-missions.ts, /opt/exilium/packages/db/src/schema/mission-center-state.ts, /opt/exilium/packages/db/src/schema/mission-definitions.ts, /opt/exilium/packages/db/src/schema/mission-reports.ts, /opt/exilium/apps/web/src/pages/Missions.tsx, /opt/exilium/apps/web/src/components/fleet/PveMissionBanner.tsx, /opt/exilium/apps/web/src/components/galaxy/GalaxySystemView/DetailPanel/ModeBelt.tsx
QUESTIONS : Faut-il brancher (ou supprimer) l'expiration 7 jours avant le rework ? Le cron n'existe pas, 55 missions 'available' pourrissent en base, et le cooldown de dismiss 24h annoncé dans l'aide n'est pas implémenté — le rework doit trancher entre corriger l'existant ou redéfinir la péremption (ex : missions des Premiers persistantes tant que le système n'est pas repris). | Exposition de enemyShips : composition exacte (déjà fuitée par getMissionById via parameters.scaledFleet) ou information graduée (FP seul → classes → détail via scan/espionnage) ? La donnée existe déjà figée à la génération ; c'est un pur choix de design d'information. | Missions de groupe : user_id nullable sur pve_missions + table de participation, ou nouvelle table world-anchored (sur le modèle asteroid_belts/deposits qui sont déjà partagés et concurrency-safe) ? Et qui découvre : cron serveur global (rupture avec le lazy actuel) ou premier joueur qui ouvre la page ? | Le niveau du centre de missions reste-t-il dérivé du niveau d'empire (empireMissionLevel, bâtiment supprimé en c7f43201), ou le recentrage « centre de missions = boucle principale » justifie-t-il de re-matérialiser un bâtiment par planète (le front garde déjà son illustration) ? | Tiers pour un système riche : aujourd'hui tirage uniforme parmi les tiers débloqués et aucun tier côté minage — faut-il une pondération par niveau, des tiers minage, et des tiers visibles sur les systèmes des Premiers (ports/prises/silos) ? | Calibrage récompenses : le butin pirate subit ×0.1 (pve_loot_multiplier) et les bonusShips ne donnent que des coques standard — pour des récompenses qualitatives (vaisseaux spéciaux, as de pilotage), étendre le mécanisme bonusShips (shipId/count/chance en JSONB) ou créer un système de récompenses dédié ? | La découverte paresseuse (catch-up au cap à l'ouverture de la page) est-elle un feature à conserver pour un jeu entre amis à cadence variable, ou les missions doivent-elles être poussées (notifications) pour devenir le rendez-vous quotidien ?

================================================================================
## Vaisseau amiral Exilium : inventaire complet du système et exigences pour une collection de vaisseaux spéciaux + carte des 7 leviers de bonus morts

- FAIT : Le schéma flagships impose STRICTEMENT un amiral par joueur (index unique sur user_id) ; chaque ligne porte : personnalisation (name 32c, description 256c, flagshipImageIndex), stats de combat/logistique dénormalisées (weapons, shield, hull, baseArmor, shotCount, baseSpeed, fuelConsumption, cargoCapacity, driveType, combatCategoryId), unlockedShips (text[]), statut + réparation (status, repairEndsAt), coque (hullId, hullChangedAt, hullChangeAvailableAt, refitEndsAt), et le stationnement via planetId NOT NULL (FK planets, set null on delete).
  PREUVE : /opt/exilium/packages/db/src/schema/flagships.ts:5-47 (uniqueIndex ligne 46)
- FAIT : Les stats de base de l'amiral = max de chaque stat sur tous les types de vaisseaux déjà construits par le joueur (min pour fuelConsumption), recalculées à chaque déblocage d'un nouveau type par le chantier ; espionageProbe, solarSatellite et recuperateur sont exclus.
  PREUVE : /opt/exilium/packages/game-engine/src/formulas/flagship-stats.ts:41-61 ; appelant : /opt/exilium/apps/api/src/modules/shipyard/shipyard.service.ts:635 → addUnlockedShip dans /opt/exilium/apps/api/src/modules/flagship/flagship.service.ts:421-456
- FAIT : Trois coques seulement, définies en config (clé universe 'hulls' du seed, type HullConfig) : combat (passifs +8 weapons/+6 armor/+2 shots, -20% build militaire, -45% temps réparation), industrial (-20% build industriel, +45% mining, +45% prospection, abilities fleet_unlock 'mine' avec extraction=soute et 'recycle'), scientific (-20% temps de recherche, ability active 'scan_mission' cooldown 1800s espionageBonus 5). Chaque coque fixe aussi le playstyle du user (warrior/miner/explorer).
  PREUVE : /opt/exilium/packages/db/src/seed-game-config.ts:357-462 ; type : /opt/exilium/apps/api/src/modules/admin/game-config.types.ts:66-82 ; playstyle écrit dans users : flagship.service.ts:208,416
- FAIT : Le système de talents a été retiré (2026-05-03) : talent.service est réduit à computeTalentContext(userId, planetId?) qui produit uniquement les passifs de coque remappés (repair_time_reduction→flagship_repair_time ; *_time_reduction→hull_* ; mining_speed_bonus→mining_speed ; prospection_speed_bonus→prospection_speed) + shipyard_parallel_build (+1 à chantier ≥10 et ≥20, seulement sur la planète où l'amiral est posé — seul bonus par-planète restant). L'ancien système survit en table morte flagship_talents_archive (168 lignes, PK flagship_id+talent_id, current_rank).
  PREUVE : /opt/exilium/apps/api/src/modules/flagship/talent.service.ts:7-84 ; SQL : \d flagship_talents_archive + SELECT count(*) FROM flagship_talents_archive → 168
- FAIT : flagship_cooldowns est déjà un mécanisme générique d'abilities : PK (flagship_id, talent_id) où talent_id stocke désormais l'id d'ability ('scan_mission'), avec activatedAt/expiresAt/cooldownEnds ; 2 lignes en prod, toutes 'scan_mission'.
  PREUVE : /opt/exilium/packages/db/src/schema/flagship-cooldowns.ts:4-14 ; commentaire flagship.service.ts:134-137 ; SQL : SELECT talent_id, count(*) FROM flagship_cooldowns GROUP BY 1 → scan_mission|2
- FAIT : Cycle de vie complet : create (unique, posé sur la planète-mère, choix de coque parmi enum ['combat','industrial','scientific']) ; rename ; changeHull (statut hull_refit 300s + cooldown 300s — coût en ressources et cooldown réel DÉSACTIVÉS par un TODO) ; réparation lazy dans get() (retour auto planète-mère) ; repair instantané payé 2 exilium (raison 'flagship_repair') ; incapacitate = statut 'incapacitated' + repairEndsAt = 7200s / (1+flagship_repair_time) + téléportation planète-mère (jamais détruit) ; auto-récupération si 'in_mission' sans fleet event actif contenant la clé flagship.
  PREUVE : /opt/exilium/apps/api/src/modules/flagship/flagship.service.ts:45-158 (get lazy), 160-211 (create), 247-275 (repair, coût ligne 263), 282-312 (incapacitate), 371-419 (changeHull, TODO lignes 383-400) ; router : flagship.router.ts:16-77
- FAIT : Intégration flotte : l'amiral voyage comme clé scalaire 'flagship' dans fleetEvents.ships (jsonb Record<string,int>) ; send-fleet valide statut 'active' + planetId === planète d'origine + ability fleet_unlock de la coque pour les missions mine/recycle, puis injecte ses stats dans shipStatsMap (vitesse/fuel/cargo/extraction=soute si miningExtractionEqualsCargo) ; setInMission au départ, returnFromMission déplace planetId vers la cible (station/colonize, missions sans retour) ou l'origine (retour classique).
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts:122-164,317 ; fleet.service.ts:365-376 (cible) et 547-550 (origine) ; station.handler.ts:104 exclut 'flagship' des colonnes planet_ships
- FAIT : Intégration combat : l'amiral est injecté comme unité unique ShipCombatConfig construite à l'arrivée depuis la ligne DB (attack et pirate), coût débris = 0 ; s'il figure dans les pertes il est incapacité (jamais détruit) + notification 'flagship-incapacitated' + retiré de la flotte de retour ; le simulateur de combat expose flagshipLossChance (fraction de runs où il tombe).
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/handlers/attack.handler.ts:142-165,297-325 ; pirate.handler.ts:33-64,71 ; combat.helpers.ts:155-167 (FP) ; combat/combat.service.ts:144-184
- FAIT : Piège d'équilibrage : les passifs de combat de la coque (+8 weapons/+6 armor/+2 shots) ne sont appliqués aux effectiveStats QUE si status==='active' (« only when stationed »), or les handlers de combat lisent effectiveStats à l'arrivée quand le statut est 'in_mission' → les bonus de stats de la coque combat ne s'appliquent jamais en combat offensif ; et l'amiral stationné ne participe pas à la défense (la flotte défenderesse = planet_ships uniquement, l'amiral défenseur n'apparaît que dans les rapports d'espionnage). Ces passifs sont donc morts en pratique.
  PREUVE : /opt/exilium/apps/api/src/modules/flagship/flagship.service.ts:127-132 (condition status==='active') vs attack.handler.ts:144-161 (lecture en mission) ; défense : attack.handler.ts:235 (defenderFleet = parseUnitRow(defShipsRow)) ; amiral défenseur seulement dans spy : flagship.service.ts:533-538
- FAIT : Config morte supplémentaire : defaultWeaponProfile (V7-WeaponProfiles) est défini pour les 3 coques dans le seed et typé dans HullConfig, mais n'est lu par AUCUN code — le champ ShipCombatConfig.weapons (profils multi-batteries) n'est jamais fourni pour l'amiral, le moteur synthétise une batterie unique.
  PREUVE : grep -rn defaultWeaponProfile → seulement seed-game-config.ts:372,400,440 + game-config.types.ts:81 (aucun consommateur) ; combat.ts:43-45
- FAIT : Levier mort fleet_speed : lu à 3 endroits, toujours en facteur multiplicatif (1 + ctx['fleet_speed']) appliqué aux speedMultipliers de chaque vaisseau — send-fleet (envoi réel), fleet-queries (estimation UI), schedule-return (vitesse du trajet retour).
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts:170 ; fleet-queries.ts:98 ; schedule-return.ts:69
- FAIT : Levier mort fleet_fuel : lu à 2 endroits, en diviseur du carburant total — fuel = fuelConsumption(...) / (1 + ctx['fleet_fuel']) — à l'envoi réel et dans l'estimation.
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts:195-196 ; fleet-queries.ts:112-113
- FAIT : Levier mort fleet_cargo : lu à 1 endroit — capacité de fret totale de la flotte multipliée par (1 + ctx['fleet_cargo']) avant validation du chargement à l'envoi.
  PREUVE : /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts:199
- FAIT : Levier mort pve_loot : lu à 2 endroits dans la génération des missions PvE (les récompenses stockées = ce que le joueur verra) — gisements des missions mine (minerai/silicium/hydrogene × (1+pve_loot)) et récompenses pirate (× rewardRatio × pve_loot_multiplier × (1+pve_loot)).
  PREUVE : /opt/exilium/apps/api/src/modules/pve/pve.service.ts:285-290 (mine) et 426-434 (pirate)
- FAIT : Levier mort market_fee : lu à 1 endroit — la commission du marché est divisée : adjustedPercent = market_commission_percent / (1 + ctx['market_fee']).
  PREUVE : /opt/exilium/apps/api/src/modules/market/market.service.ts:63-70 (ligne 67)
- FAIT : Leviers morts military_build_time et industrial_build_time : lus via une clé CONSTRUITE par ternaire sur la catégorie du vaisseau (invisibles à un grep naïf), en multiplicateur (1 - ctx[key]) du temps de construction, à 4 endroits du shipyard (listing, enqueue, et 2 autres chemins de calcul du temps) — toujours empilés avec les clés hull_combat/industrial_build_time_reduction qui, elles, ont un producteur (les coques).
  PREUVE : /opt/exilium/apps/api/src/modules/shipyard/shipyard.service.ts:101-126 (site 1), 475-505, 726-757, 862-875 ; explication du ternaire : admin/__tests__/bonus-levers.test.ts:52-57
- FAIT : Le test D2 bonus-levers.test.ts est le garde-fou anti-régression du rework : il croise statiquement clés LUES (motif talentCtx/ctx['...']) vs clés PRODUITES (seed stat:, passiveBonuses remappés, écritures littérales de computeTalentContext) ; les 7 leviers sont figés dans LEVIERS_MORTS_CONNUS et le test EXIGE de les retirer de la liste dès qu'un producteur apparaît — brancher les vaisseaux spéciaux dessus = écrire ces clés dans computeTalentContext (ou équivalent) et mettre à jour la liste.
  PREUVE : /opt/exilium/apps/api/src/modules/admin/__tests__/bonus-levers.test.ts:46-57 (liste), 99-125 (producteurs reconnus), 159-167 (test de résurrection)
- FAIT : Pour « multiplier l'amiral en collection » : la table actuelle est verrouillée mono-ligne (index unique user_id) et ~30 sites font select .where(byUser(flagships.userId)).limit(1) — le chemin propre est une NOUVELLE table special_ships (1 ligne par vaisseau : user_id, definition_id, planet_id nullable pour la pose, status, repair_ends_at, acquired_at ; unicité par UNIQUE(user_id, definition_id) pour un exemplaire par type) plutôt que d'assouplir flagships ; les patterns à généraliser existent tous déjà : cooldowns par (ship_id, ability_id) = flagship_cooldowns, pose-sur-planète = planetId + comparaison flagship.planetId===planetId de talent.service:72, réparation = status+repairEndsAt, abilities data-driven en config = hulls[].abilities.
  PREUVE : flagships.ts:46 (unicité) ; talent.service.ts:71-81 (hook par-planète) ; flagship-cooldowns.ts:12-13 ; seed-game-config.ts:403-419,444-453 (abilities)
- FAIT : Rattachement à une flotte : fleetEvents.ships est un Record<string,int> — un vaisseau spécial individuel n'y est PAS adressable nativement ; le pattern amiral (clé réservée 'flagship' + validation dédiée à l'envoi + injection shipStatsMap/shipCombatConfigs à l'arrivée + retrait des colonnes planet_ships dans station) devra être généralisé, p.ex. clés 'special:<definition_id>' ou colonne jsonb specialShips sur fleet_events ; la boucle récompense existe déjà : pveMissions.rewards contient déjà bonusShips {shipId,count,chance} accordés au retour — accorder un vaisseau spécial = insérer une ligne special_ships au lieu d'incrémenter planet_ships.
  PREUVE : send-fleet.ts:122-164 (pattern clé réservée) ; station.handler.ts:104 ; pve.service.ts:419-435 (bonusShips) ; fleet.service.ts:547-560 (merge bonus ships au retour)
- FAIT : État prod : 14 amirals (5 combat dont 1 en mission, 9 industrial dont 1 en mission, 0 scientific — la coque scan n'est utilisée par personne), 12 actifs/posés ; population minuscule, une migration de flagships vers une collection est peu risquée côté données.
  PREUVE : SQL : SELECT hull_id, status, count(*) FROM flagships GROUP BY 1,2 → combat/active 4, combat/in_mission 1, industrial/active 8, industrial/in_mission 1
FICHIERS CLÉS : /opt/exilium/packages/db/src/schema/flagships.ts, /opt/exilium/packages/db/src/schema/flagship-cooldowns.ts, /opt/exilium/apps/api/src/modules/flagship/flagship.service.ts, /opt/exilium/apps/api/src/modules/flagship/talent.service.ts, /opt/exilium/apps/api/src/modules/flagship/flagship.router.ts, /opt/exilium/packages/db/src/seed-game-config.ts, /opt/exilium/apps/api/src/modules/admin/game-config.types.ts, /opt/exilium/packages/game-engine/src/formulas/flagship-stats.ts, /opt/exilium/apps/api/src/modules/fleet/operations/send-fleet.ts, /opt/exilium/apps/api/src/modules/fleet/operations/fleet-queries.ts, /opt/exilium/apps/api/src/modules/fleet/operations/schedule-return.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/attack.handler.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/pirate.handler.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/mine.handler.ts, /opt/exilium/apps/api/src/modules/fleet/combat.helpers.ts, /opt/exilium/apps/api/src/modules/fleet/fleet.service.ts, /opt/exilium/apps/api/src/modules/shipyard/shipyard.service.ts, /opt/exilium/apps/api/src/modules/pve/pve.service.ts, /opt/exilium/apps/api/src/modules/market/market.service.ts, /opt/exilium/apps/api/src/modules/research/research.service.ts, /opt/exilium/apps/api/src/modules/admin/__tests__/bonus-levers.test.ts
QUESTIONS : Les vaisseaux spéciaux remplacent-ils l'amiral (flagships devient special_ships, migration des 14 lignes) ou coexistent-ils avec lui (l'amiral reste le « héros » unique et la collection s'ajoute à côté) ? | Unicité des vaisseaux spéciaux : un exemplaire par type ET par joueur (UNIQUE(user_id, definition_id)), ou des exemplaires uniques à l'échelle de l'univers coopératif (premier arrivé premier servi sur une mission) ? | Un vaisseau spécial peut-il être posé sur une planète ET rattaché à une flotte en même temps (deux états exclusifs comme l'amiral : planetId quand posé, clé dans fleetEvents.ships quand en mission), et ses bonus planétaires doivent-ils s'éteindre quand il part en mission (sémantique actuelle : passifs de coque globaux et permanents, seule shipyard_parallel_build est liée à la planète de pose) ? | Les définitions de vaisseaux spéciaux vivent-elles en config (comme hulls dans universe_config, éditables via l'admin) ou en table dédiée (comme mission_definitions) ? | Faut-il corriger au passage les passifs de combat de coque jamais appliqués en combat (condition status==='active' vs lecture en 'in_mission') et le defaultWeaponProfile jamais consommé, ou considérer que la refonte en vaisseaux spéciaux les remplace purement ? | Les bonus des vaisseaux spéciaux s'agrègent-ils via computeTalentContext étendu (les 30 sites consommateurs marchent alors sans modification, et le test D2 reconnaît déjà ses écritures littérales comme producteurs) ou via un nouveau service dédié (qui exigerait d'apprendre au test D2 un nouveau motif de producteur) ? | Le levier market_fee garde-t-il un sens dans le recentrage coop (le marché reste « en soutien ») ou faut-il retirer la lecture plutôt que produire la clé ? | Perte d'un vaisseau spécial en combat : incapacitation + réparation comme l'amiral (jamais perdu), ou perte définitive pour donner du poids aux missions dangereuses ?

================================================================================
## Fil de l'univers et faits d'armes — état des lieux événements/notifications/ranking d'Exilium

- FAIT : Les événements persistés vivent dans la table game_events, strictement PAR JOUEUR : user_id NOT NULL (cascade), planet_id nullable, type texte libre, payload jsonb, flag read, index (user, read, date) — aucun événement global/public n'est représentable aujourd'hui.
  PREUVE : /opt/exilium/packages/db/src/schema/game-events.ts:5-16
- FAIT : Rétention 30 jours : le cron event-cleanup (24h) supprime tout game_event de plus de 30 jours — incompatible tel quel avec des faits d'armes permanents ou un fil d'univers historique.
  PREUVE : /opt/exilium/apps/api/src/modules/game-event/game-event.service.ts:93-101 + /opt/exilium/apps/api/src/workers/worker.ts:121
- FAIT : Exactement 14 types d'événements sont persistés (building/research/shipyard-done, fleet-arrived/returned, pve-mission-done, tutorial-quest-done, friend-request/accepted/declined, report-sold/purchased, market-offer-reserved/sold) ; tout le reste (~14 types de plus : fleet-inbound, fleet-attack-landed, flagship-incapacitated, daily-quest-completed, empire-level-up, market-offer-expired, messages, alliance…) n'est qu'un SSE éphémère jamais stocké.
  PREUVE : /opt/exilium/apps/api/src/modules/game-event/game-event.service.ts:8 (GameEventType) vs /opt/exilium/packages/shared/src/types/notifications.ts:30-55 (EVENT_TYPE_TO_CATEGORY) et le switch de /opt/exilium/apps/web/src/hooks/useNotifications.ts:91-393
- FAIT : Trois canaux distincts existent déjà : SSE temps réel (Redis pub/sub canal `notifications:{userId}` → GET /sse, un canal PAR utilisateur, aucun canal broadcast), persistance game_events (cloche + historique), et Web Push VAPID (push_subscriptions), chacun filtrable par notification_preferences (toastDisabled/pushDisabled/bellDisabled).
  PREUVE : /opt/exilium/apps/api/src/modules/notification/notification.sse.ts:43-44, /opt/exilium/apps/api/src/modules/push/push.service.ts:39-49, /opt/exilium/apps/api/src/modules/notification/notification-preferences.service.ts:5-9
- FAIT : Les émissions sont dispersées : workers BullMQ (fleet.worker et build-completion.worker font publishNotification + push + insert game_events dans un pipeline post-complétion), et services applicatifs (friend, trade.handler, market, message, daily-quest, empire-progression) — il n'y a PAS de bus d'événements central, chaque site d'émission choisit ses canaux à la main.
  PREUVE : /opt/exilium/apps/api/src/workers/fleet.worker.ts:59-116, /opt/exilium/apps/api/src/workers/build-completion.worker.ts:53-84, grep publishNotification → 25+ sites
- FAIT : En prod la table game_events ne contient que 32 lignes, toutes de type building-done (2026-07-08 → 2026-08-09) ; rankings a 25 lignes, alliance_logs et announcements sont vides — le volume est trivial, aucune contrainte de migration de données.
  PREUVE : sudo -u postgres psql -d exilium -c "SELECT type, count(*) FROM game_events GROUP BY type;" et counts rankings/alliance_logs/announcements
- FAIT : Affichage web actuel : cloche topbar (gameEvent.recent 10 derniers + unreadCount, markAllRead à l'ouverture), page /history (curseur infini + filtres par type pilotés par 14 ui_labels 'event.*' en base), accordéon OverviewEvents sur l'Overview, et invalidations React-Query pilotées par le SSE — tout est privé au joueur connecté.
  PREUVE : /opt/exilium/apps/web/src/components/layout/topbar/NotificationsBell.tsx:20-32, /opt/exilium/apps/web/src/pages/History.tsx:24-26, SELECT key FROM ui_labels WHERE key LIKE 'event.%' (14 lignes)
- FAIT : Le SEUL fil partagé multi-joueurs existant est le fil d'activité d'alliance : table alliance_logs (payloads Zod discriminés en 9 types combat./espionage./member., visibilité all/officers, purge 30j), pagination par curseur temporel, unread via allianceMembers.activitySeenAt, fan-out SSE léger 'alliance-log:new' par membre — c'est le prior art architectural direct pour le fil de l'univers (à ceci près qu'un fil univers = broadcast à tous, ce que le SSE par-user ne sait pas faire sans canal global ou polling).
  PREUVE : /opt/exilium/packages/shared/src/alliance-log.ts:100-119, /opt/exilium/apps/api/src/modules/alliance/alliance-log.service.ts:17-68, /opt/exilium/apps/api/src/modules/alliance/alliance.service.ts:538-585, /opt/exilium/apps/api/src/cron/alliance-log-purge.ts:4
- FAIT : La découverte de biome est purement privée : discovered_biomes et discovered_positions sont clé-primaire (userId, coords, biomeId), l'explore.handler insère en onConflictDoNothing et ne produit qu'un rapport privé — AUCUN événement de découverte n'est émis et la notion de « premier de l'univers à découvrir ce biome » n'existe nulle part (elle est pourtant calculable a posteriori : première ligne discovered_biomes par biomeId toutes users confondues, si on ajoute un created_at qui manque à discovered_biomes).
  PREUVE : /opt/exilium/packages/db/src/schema/biomes.ts (discoveredBiomes sans timestamp), /opt/exilium/apps/api/src/modules/fleet/handlers/explore.handler.ts:130-140
- FAIT : « Qui a débloqué quoi » : research-done est stocké par joueur avec payload techId/level (matière première exploitable) mais reste privé ; aucun concept de silo/recherche-rendue-à-tous, ni d'événement public de déblocage n'existe.
  PREUVE : /opt/exilium/apps/api/src/workers/build-completion.worker.ts:79-84 + GameEventType 'research-done'
- FAIT : « Où en est le front » : rien n'existe — pas de systèmes tenus par les Premiers ni de secteurs ports/prises/silos ; le PvE actuel est instancié PAR JOUEUR (pve_missions avec user_id, pirate lairs/asteroid belts) donc le front exige un état-univers neuf (progression par système) + des événements de bascule (port pris/perdu, prise conquise, silo libéré) qui n'ont aucun équivalent actuel.
  PREUVE : /opt/exilium/packages/db/src/schema/pve-missions.ts:3-16 (userId NOT NULL), ls modules/galaxy (pas de notion de contrôle de système)
- FAIT : Le ranking actuel est un pur classement de puissance individuel : cron toutes les 30 min recalcule totalPoints = coûts cumulés investis (bâtiments+recherche+flotte+défenses) / ranking_points_divisor, trié en rang ; exposé via ranking.list et ranking.me, plus alliance.ranking (somme des points des membres).
  PREUVE : /opt/exilium/apps/api/src/modules/ranking/ranking.service.ts:16-117, /opt/exilium/apps/api/src/workers/worker.ts:118, /opt/exilium/apps/api/src/modules/alliance/alliance.service.ts (async ranking, sum totalPoints)
- FAIT : À débrancher pour supprimer les classements de puissance : pages web /ranking et /alliance-ranking (seuls consommateurs de trpc.ranking.* et trpc.alliance.ranking, référencés dans BottomTabBar onglet social et AlliancePublic), le routeur ranking, le cron ranking-update — le profil joueur n'affiche pas de rang, donc le découplage est propre ; en revanche AUCUN score collectif n'existe aujourd'hui (rien à garder, tout à créer : le pipeline de recalcul périodique sous verrou Redis est le seul réutilisable).
  PREUVE : grep trpc.ranking → uniquement Ranking.tsx/AllianceRanking.tsx ; grep rank PlayerProfile.tsx → 0 résultat ; /opt/exilium/apps/web/src/components/layout/BottomTabBar.tsx:26
- FAIT : Le système d'annonces (announcements, 280 chars, variant info/warning/success, 1 seule active, bannière dismissable en sessionStorage, lien changelog) est top-down admin-only — utilisable pour de la narration ponctuelle des Premiers mais pas pour un fil.
  PREUVE : /opt/exilium/packages/db/src/schema/announcements.ts, /opt/exilium/apps/api/src/modules/announcement/announcement.service.ts:10-24, /opt/exilium/apps/web/src/components/layout/AnnouncementBanner.tsx
- FAIT : Pour les faits d'armes durables, empire_xp_log (userId, amount, source, details jsonb, created_at, sans purge cron repérée) est le seul journal per-user sans rétention 30j — mais il ne trace que l'XP d'empire ; il manque une table d'accomplissements pérenne (premières fois, records qualitatifs, missions du Centre accomplies) + un écran type « états de service » sur le profil.
  PREUVE : /opt/exilium/packages/db/src/schema/empire-progression.ts (empireXpLog), grep des crons dans worker.ts (aucune purge empire_xp_log)
- FAIT : Ce que le fil de l'univers exige concrètement en creux de l'existant : (1) une table nouvelle type universe_events (scope global, acteur nullable, type+payload jsonb, rétention longue ou saisonnière) car game_events est per-user et purgé à 30j ; (2) des émissions nouvelles aux points déjà identifiés — explore.handler (première découverte de biome au niveau univers), complétion de recherche (déblocages notables/silos), futurs handlers de secteurs Premiers (bascules de front) ; (3) un canal de diffusion broadcast (canal Redis global relayé par le SSE existant, ou simple polling du fil) ; (4) un écran fil + un état agrégé du front, sur le modèle éprouvé ActivityFeed/alliance.activity (curseur temporel + catégories + unread par lastSeen).
  PREUVE : Synthèse des constats ci-dessus : game-events.ts:7 (user_id NOT NULL), notification.sse.ts:43 (canal par user), alliance.service.ts:538 (pattern feed), explore.handler.ts:130
FICHIERS CLÉS : /opt/exilium/packages/db/src/schema/game-events.ts, /opt/exilium/apps/api/src/modules/game-event/game-event.service.ts, /opt/exilium/apps/api/src/modules/game-event/game-event.router.ts, /opt/exilium/packages/shared/src/types/notifications.ts, /opt/exilium/apps/api/src/modules/notification/notification.publisher.ts, /opt/exilium/apps/api/src/modules/notification/notification.sse.ts, /opt/exilium/apps/api/src/modules/notification/notification-preferences.service.ts, /opt/exilium/apps/api/src/modules/push/push.service.ts, /opt/exilium/apps/api/src/workers/worker.ts, /opt/exilium/apps/api/src/workers/fleet.worker.ts, /opt/exilium/apps/api/src/workers/build-completion.worker.ts, /opt/exilium/apps/api/src/cron/event-cleanup.ts, /opt/exilium/apps/api/src/cron/alliance-log-purge.ts, /opt/exilium/apps/api/src/modules/ranking/ranking.service.ts, /opt/exilium/apps/api/src/modules/ranking/ranking.router.ts, /opt/exilium/packages/shared/src/alliance-log.ts, /opt/exilium/apps/api/src/modules/alliance/alliance-log.service.ts, /opt/exilium/apps/api/src/modules/alliance/alliance.service.ts, /opt/exilium/apps/api/src/modules/announcement/announcement.service.ts, /opt/exilium/apps/api/src/modules/fleet/handlers/explore.handler.ts, /opt/exilium/packages/db/src/schema/biomes.ts, /opt/exilium/packages/db/src/schema/empire-progression.ts, /opt/exilium/apps/web/src/hooks/useNotifications.ts, /opt/exilium/apps/web/src/components/layout/topbar/NotificationsBell.tsx, /opt/exilium/apps/web/src/pages/History.tsx, /opt/exilium/apps/web/src/pages/Ranking.tsx, /opt/exilium/apps/web/src/lib/game-events.ts, /opt/exilium/apps/web/src/components/layout/AnnouncementBanner.tsx
QUESTIONS : Diffusion du fil : canal Redis broadcast unique relayé par le SSE existant (nouveau subscribe `notifications:universe` dans notification.sse.ts) ou simple polling 30-60s du fil — le polling suffit-il vu le rythme de jeu ? | Rétention du fil de l'univers : permanente, ou alignée sur les saisons (« carte neuve au lancement ») avec archivage — et faut-il exempter les faits d'armes de toute purge ? | Faits d'armes : catalogue typé en dur (table achievements avec définitions, à la manière des payloads Zod d'alliance-log) ou flux libre dérivé du fil univers filtré par acteur — quel niveau de garantie de schéma veut-on ? | Score collectif : quelle métrique canonique (secteurs pris, silos libérés, exilium cumulé de l'univers ?) et remplace-t-il la page /ranking ou vit-il dans l'écran du front ? | Premières découvertes : discovered_biomes n'a pas de created_at — ajouter le timestamp et backfiller, ou repartir de zéro à la carte neuve (auquel cas aucun backfill nécessaire) ? | Faut-il débrancher le cron ranking-update immédiatement avec les pages, ou le laisser tourner tant que alliance.ranking (somme des points) reste affiché quelque part ? | L'admin doit-il pouvoir injecter des événements scénarisés des Premiers dans le fil (extension du back-office announcements ou nouvelle entrée admin sur universe_events) ? | Un événement public par recherche terminée serait très bavard : quel seuil de notabilité (capstones/forks S1, silos uniquement, premier joueur à atteindre un palier) pour le « qui a débloqué quoi » ?

================================================================================
## Shell front Exilium : composition de l'écran principal, impact d'un « Centre de missions = home », et livraison staging-only

- FAIT : Le routing est un createBrowserRouter (react-router v7) unique : '/' rend RootSwitch (Landing si non connecté, sinon Layout) et l'écran principal actuel est la route index qui lazy-charge pages/Overview.tsx ; /missions est une route enfant plate qui charge pages/Missions.tsx.
  PREUVE : /opt/exilium/apps/web/src/router.tsx:66-101 (index → Overview), :212-215 (/missions)
- FAIT : Le pattern de redirection de routes héritées est déjà établi et réutilisable pour basculer la home : /energy et /resources → Navigate vers '/', /shipyard → '/production?tab=vaisseaux', etc. — changer la home = échanger le composant de la route index, ou rediriger '/' vers /missions.
  PREUVE : /opt/exilium/apps/web/src/router.tsx:113-123, 144-155
- FAIT : Layout.tsx compose le shell : Sidebar desktop (fixe lg:), colonne principale avec TopBar (mobile only), bannières (Offline/EmailVerification/Announcement), PlanetSubnav (header desktop sticky : sélecteur de planète + 4 ressources), HostileAlertBanner, <main><Outlet context={{planetId, planetClassId}}></main>, puis BottomTabBar (mobile, sibling flex non-fixed).
  PREUVE : /opt/exilium/apps/web/src/components/layout/Layout.tsx:71-114
- FAIT : activePlanetId vit dans un store zustand adossé à localStorage (clé 'activePlanetId'), MAIS depuis le fix du finding friction-bots il est aussi miroité bidirectionnellement dans l'URL (?planet=<id>) par useActivePlanetUrl, monté une fois dans Layout — le store reste la source de vérité, l'URL est un pont (commit 2a03ec65 « adressabilité URL — planète active + sous-vues R6/R7/R8 »).
  PREUVE : /opt/exilium/apps/web/src/stores/planet.store.ts:9-21 ; /opt/exilium/apps/web/src/hooks/useActivePlanetUrl.ts:10-18 ; Layout.tsx:45-47 ; git log 2a03ec65
- FAIT : L'Overview actuel (519 lignes) est lourdement planète-scopé : ~13 requêtes tRPC dont 8 clé-ées sur planetId (resource.production, building.list, shipyard.queue/ships/defenses, gameEvent.byPlanet, colonization.status), et il intercepte lui-même l'état 'colonizing' pour rendre ColonizationProgress à la place.
  PREUVE : /opt/exilium/apps/web/src/pages/Overview.tsx:328-384
- FAIT : La page Missions est au contraire empire-wide : trpc.pve.getMissions ne prend aucun planetId (le centerLevel vient de la réponse), donc en faire la home ne dépend pas d'activePlanetId ; ses CTA naviguent vers /fleet/send?mission=…&pveMissionId=… en query params (déjà adressable).
  PREUVE : /opt/exilium/apps/web/src/pages/Missions.tsx:99, 333, 506
- FAIT : Navigation desktop : Sidebar.tsx a 3 sections statiques (Planète: '/', /buildings, /production ; Empire: /empire, /politiques, /research ; Galaxie: /galaxy, /fleet, /missions, /market), filtrées par getVisibleSidebarPaths du game-engine selon le chapitre de tutoriel et le nombre de colonies.
  PREUVE : /opt/exilium/apps/web/src/components/layout/Sidebar.tsx:28-56, 72-75
- FAIT : Gating tutoriel : '/missions' n'est visible qu'à partir du chapitre 3 alors que '/' est 'always' — si le Centre de missions devient l'écran principal, la règle SIDEBAR_VISIBILITY_RULES doit changer ou la home doit avoir un fallback pour les chapitres 1-2, sinon les nouveaux joueurs ont une home invisible dans leur propre nav.
  PREUVE : /opt/exilium/packages/game-engine/src/sidebar-visibility.ts:22-39 ('/': always, '/missions': atChapter(3))
- FAIT : Navigation mobile : BottomTabBar.tsx a 5 onglets (Empire/Planète/Galaxie/Flotte/Social) ouvrant des BottomSheets ; Missions est un sous-item de la sheet 'galaxie' ; getActiveTab a un cas spécial pathname === '/' → groupe 'planete'. Faire de /missions la home touche TAB_GROUPS, SHEET_ITEMS et ce cas spécial — et la BottomTabBar n'applique PAS le gating tutoriel de la sidebar (bug de parité déjà vu : Politiques oublié sur mobile, détecté par D3).
  PREUVE : /opt/exilium/apps/web/src/components/layout/BottomTabBar.tsx:21-71 (TAB_GROUPS, SHEET_ITEMS, getActiveTab), :34-36 (commentaire Politiques inatteignable)
- FAIT : Layout redirige les pages planète-scopées vers '/' quand la planète active est en colonisation (PLANET_PAGES = ['/', '/resources', '/infrastructures', '/energy', '/production']) et c'est Overview qui affiche ColonizationProgress — si '/' devient le Centre de missions (empire-wide), cette mécanique de redirection + l'interception colonizing doivent être re-pointées (ex. vers une route /planete dédiée).
  PREUVE : /opt/exilium/apps/web/src/components/layout/Layout.tsx:21-24, 49-60 ; Overview.tsx:384
- FAIT : La PWA a start_url '/' dans son manifest (registerType autoUpdate) : changer ce que '/' rend change l'écran d'ouverture de l'app installée sans autre modif, mais tout choix de déplacer Overview vers une nouvelle route doit préserver '/' comme entrée valide.
  PREUVE : /opt/exilium/apps/web/vite.config.ts:14-24
- FAIT : Il n'existe AUCUNE infra de feature-flag côté web : zéro usage d'import.meta.env dans apps/web/src, pas de table de flags ; le seul canal runtime existant est gameConfig.getAll (publicProcedure consommée partout via useGameConfig, staleTime 5 min) adossé aux tables de config (universe_config est du key/value), qui pourrait porter une clé de flag si besoin.
  PREUVE : grep import.meta.env apps/web/src → 0 résultat ; /opt/exilium/apps/api/src/modules/admin/game-config.router.ts:721 (getAll: publicProcedure) ; SELECT key FROM universe_config (key/value : speed, galaxies, …)
- FAIT : Le mécanisme canonique « staging seulement » existe déjà et ne demande aucun flag : staging est un checkout git séparé (/opt/exilium-staging) avec sa DB exilium_staging, ses process PM2 et son vhost Caddy staging.exilium-game.com servant /opt/exilium-staging/apps/web/dist ; scripts/deploy-staging.sh <ref> déploie n'importe quelle branche/SHA — on peut donc déployer la branche du nouveau shell sur staging sans jamais la merger dans main (prod reste sur main).
  PREUVE : /opt/exilium/scripts/deploy-staging.sh:20-33 (REF=${1:-origin/main}, checkout -f $REF) ; /etc/caddy/Caddyfile:95-121
- FAIT : La leçon Passerelle est traçable dans l'historique : le shell RTS à panneaux a été livré en 2 commits (5182d450 P1, 09b4ee3e P2+P3) puis rollback en 2 reverts le même cycle (7b5d234f, c7fbeae2) — précédent direct justifiant branche + staging + retours joueurs avant merge pour ce nouveau changement de paradigme, sans l'entremêler avec le gameplay Premiers/systèmes.
  PREUVE : git log --grep=passerelle : 0b36bcfe (proposal), 5182d450, 09b4ee3e, 7b5d234f (revert), c7fbeae2 (revert #2)
- FAIT : Le shell actuel est déjà passé par une refonte nav « 1 seul menu » (onglets planète fusionnés dans la sidebar, Alliance/Classement migrés sous l'avatar, ressources+flux fusionnés dans PlanetSubnav) — le point d'insertion naturel d'un hub Missions en home est donc : route index + section sidebar remontée + onglet mobile dédié, pas un nouveau système de panneaux.
  PREUVE : git 71da0401 ; /opt/exilium/apps/web/src/components/layout/PlanetSubnav.tsx:54-62 (commentaire refonte) ; Sidebar.tsx:54-56
FICHIERS CLÉS : /opt/exilium/apps/web/src/router.tsx, /opt/exilium/apps/web/src/components/layout/Layout.tsx, /opt/exilium/apps/web/src/components/layout/BottomTabBar.tsx, /opt/exilium/apps/web/src/components/layout/Sidebar.tsx, /opt/exilium/apps/web/src/components/layout/PlanetSubnav.tsx, /opt/exilium/apps/web/src/pages/Overview.tsx, /opt/exilium/apps/web/src/pages/Missions.tsx, /opt/exilium/apps/web/src/stores/planet.store.ts, /opt/exilium/apps/web/src/hooks/useActivePlanetUrl.ts, /opt/exilium/apps/web/src/hooks/activePlanetUrl.logic.ts, /opt/exilium/packages/game-engine/src/sidebar-visibility.ts, /opt/exilium/scripts/deploy-staging.sh, /opt/exilium/apps/web/vite.config.ts, /opt/exilium/apps/api/src/modules/admin/game-config.router.ts, /opt/exilium/apps/web/src/stores/ui.store.ts
QUESTIONS : Si '/' devient le Centre de missions, où va l'Overview planète : nouvelle route /planete (avec redirection de PLANET_PAGES et de l'interception 'colonizing' vers elle), ou reste-t-il à '/' avec /missions promu ailleurs dans la nav ? | Que voient les nouveaux joueurs chapitres 1-2 du tutoriel si la home est le Centre de missions ('/missions' est gated atChapter(3)) : fallback Overview, ou refonte du gating tutoriel avec le nouveau shell ? | Le header desktop PlanetSubnav (ressources de la planète active, sticky sur toutes les pages) garde-t-il son sens sur une home empire-wide, ou le hub Missions appelle-t-il un header agrégé empire ? | Faut-il profiter du chantier pour aligner la BottomTabBar mobile sur getVisibleSidebarPaths (aujourd'hui les sheets mobiles ignorent le gating tutoriel — source du bug Politiques D3) ? | Le futur hub Missions (ports/prises/silos des Premiers) sera-t-il purement empire-wide comme pve.getMissions aujourd'hui, ou certaines actions redeviendront planète-scopées (départ de flotte depuis la planète active) — ce qui déterminera s'il consomme ?planet= ? | Un flag runtime est-il souhaité en plus de la livraison branche→staging (le canal existant serait une clé universe_config exposée via gameConfig.getAll, publicProcedure déjà consommée partout), ou la leçon Passerelle (branche non mergée + deploy-staging.sh <ref>) suffit-elle ?

================================================================================
## Onboarding Exilium : autopsie du tutoriel 23 quêtes (86 % d'abandon, 10 comptes scellés) et réutilisabilité du moteur pour les missions d'introduction

- FAIT : Le taux d'abandon de 86 % est exact : 22 comptes ont une ligne tutorial_progress (créée au premier chargement du jeu), mais seuls 3 joueurs (Flow, Elcorones, Roukinax) ont réellement terminé la version 23 quêtes — 19/22 = 86,4 % ; les 10 autres comptes 'is_complete' ont fini l'ancienne version 16 quêtes et n'ont jamais vu le chapitre 4.
  PREUVE : SELECT is_complete, count(*) FROM tutorial_progress GROUP BY 1 (13 t / 9 f) + SELECT username, current_quest_id, jsonb_array_length(completed_quests) FROM tutorial_progress WHERE is_complete (10 lignes à quest_16/16 quêtes, 3 à quest_23/23 quêtes)
- FAIT : Mécanisme d'enfermement exact des 10 comptes (Eyfie, Zecharia, NKh, Jamynator, Niros, Hellaynae, Vladimirovitch, zechapeon, Kém, JMFion) : ils ont fini le tutoriel 16 quêtes avant le 2026-03-29 ; le chapitre 4 (quêtes 17-23) a été semé le 2026-04-01 (commit 52169b93) ; comme `tutorialComplete = !nextQuest` avait posé is_complete=true, getCurrent court-circuite (ligne 179) et completeCurrentQuest refuse ('tutorial_already_complete', ligne 302) — aucun chemin de reprise n'existe, donc les 15 exilium + 5 interceptors du chapitre 4 leur sont définitivement inaccessibles.
  PREUVE : /opt/exilium/apps/api/src/modules/tutorial/tutorial.service.ts:179,302,397-398 + git log -S chapter_4 (52169b93, 2026-04-01) + SELECT reward_exilium, reward_units FROM tutorial_chapters WHERE id='chapter_4'
- FAIT : Le blocage de progression UI est un masquage de sidebar piloté par le chapitre du tutoriel : au chapitre 1 le joueur ne voit que Vue d'ensemble + Bâtiments ; Recherche/Chantier arrivent au ch.2, Galaxie/Flotte/Missions au ch.3, et Marché/Alliance/Classement/Colonies/Politiques exigent is_complete — un joueur bloqué ne verra donc jamais la moitié du jeu.
  PREUVE : /opt/exilium/packages/game-engine/src/sidebar-visibility.ts:20-39 (SIDEBAR_VISIBILITY_RULES) consommé par /opt/exilium/apps/web/src/components/layout/Sidebar.tsx:59-75
- FAIT : Ce masquage est incohérent : aucune garde de route dans router.tsx (l'URL directe marche, le verrou est purement de découvrabilité) et la BottomTabBar mobile n'applique aucune règle de visibilité — sur téléphone, tout est visible dès la première seconde, y compris ce que le tutoriel prétend déverrouiller.
  PREUVE : grep isComplete /opt/exilium/apps/web/src/router.tsx (aucune garde) + /opt/exilium/apps/web/src/components/layout/BottomTabBar.tsx:21-56 (TAB_GROUPS/SHEET_ITEMS statiques, zéro référence tutorial)
- FAIT : La chaîne est strictement linéaire mono-pointeur (current_quest_id, suivant = order+1, pas de skip, pas de quêtes parallèles) et chaque quête exige un clic manuel 'Suivant' dans un dropdown du topbar ; deux comptes (Adolfo à quest_1, Ped à quest_8) sont figés en pending_completion=true — condition remplie mais récompense jamais réclamée, la friction du clic suffit à stopper la progression.
  PREUVE : tutorial.service.ts:397 (nextQuest par order+1) + SELECT current_quest_id, pending_completion FROM tutorial_progress WHERE NOT is_complete (quest_1/t et quest_8/t) + OnboardingButton.tsx (bouton 'Suivant' dans dropdown)
- FAIT : Le mur d'abandon réel est à la toute première action : 5 des 22 entrants (23 %) n'ont jamais complété quest_1 'construire une mine de minerai niveau 1' — le tutoriel n'existe que comme étoile ambre 'Objectifs' dans le topbar, et la première connexion atterrit sur Overview sans aucun accueil (pas de modal, pas d'intro, aucune présence tutoriel dans Overview.tsx).
  PREUVE : Funnel SQL par quête (quest_1 : 17/22) + grep tutorial dans apps/web/src/pages : Overview.tsx absent de la liste ; seul point d'entrée = /opt/exilium/apps/web/src/components/layout/topbar/OnboardingButton.tsx
- FAIT : Le renumbering du 2026-04-01 (commit 998e6560 'remove recuperateur quest, renumber') a corrompu les historiques en cours : Taldia (15 quêtes faites, bloquée à quest_16 'mission de minage') a quest_13 'mission_complete mine' marquée faite alors qu'elle n'a AUCUNE pve_mission complétée (4 mine 'available' seulement) — les completed_quests référencent des questId réécrits sous ses pieds.
  PREUVE : SELECT mission_type, status FROM pve_missions WHERE user=Taldia (4 available, 0 completed) vs completed_quests contenant quest_13 ; git show 998e6560
- FAIT : Bug résiduel : quest_22 et quest_23 sont des doublons exacts ('Bapteme du feu', mission_complete pirate 1) — la 23e s'auto-valide dès la 22e réclamée (le service pré-vérifie la quête suivante, lignes 439-447), le joueur clique deux fois 'Suivant' sur la même quête ; et le front code en dur des questId (quest_11, quest_12, quest_17 dans OnboardingButton) désynchronisés du renumbering (le deep-link minage de quest_17 pointe sur une quête devenue 'chantier niveau 3').
  PREUVE : SELECT id, title, condition_type FROM tutorial_quest_definitions WHERE quest_order IN (22,23) + tutorial.service.ts:439-447 + OnboardingButton.tsx getActionLink (cas quest_17 → mission=mine)
- FAIT : OUI, le moteur de quêtes peut porter des 'missions d'introduction' du centre de missions : les définitions sont 100 % data-driven en DB (7 condition_types dont mission_complete branché directement sur pve_missions.mission_type — 'mine' et 'pirate' déjà utilisés), la vérification est double (event-driven via checkAndComplete appelé des workers + polling auto-réparateur checkCompletion à chaque getCurrent, qui rattrape tout événement raté), et les récompenses couvrent déjà ressources + exilium + UNITÉS (rewardUnits jsonb injecte des vaisseaux en increment atomique) — les récompenses qualitatives du recentrage (vaisseaux spéciaux) tiennent dans ce mécanisme existant.
  PREUVE : tutorial.service.ts:64-142 (getCurrentProgress), 364-380 (rewardUnits atomique), 383-385 (exiliumService.earn) ; call-sites : /opt/exilium/apps/api/src/workers/build-completion.worker.ts:87, /opt/exilium/apps/api/src/workers/fleet.worker.ts:121, /opt/exilium/apps/api/src/modules/flagship/flagship.router.ts:26
- FAIT : Trois limites du moteur à corriger pour le centre de missions : (1) le mono-pointeur interdit les objectifs concurrents — il faut une ligne par mission-instance au lieu de current_quest_id ; (2) is_complete scelle le compte pour toujours (cause de l'enfermement des 10) — un flux de missions n'a pas de 'fin' ; (3) l'identité par questId ordonné casse au moindre renumbering — il faut des id stables et un ordre séparé.
  PREUVE : tutorial_progress schema (current_quest_id varchar unique par user, is_complete boolean) + les deux pathologies observées en base (10 comptes scellés, historique Taldia corrompu)
- FAIT : Pour la première connexion 5 minutes sur carte neuve, l'outillage front existe déjà : useTutorialHighlight/useTutorialTargetId surlignent la cible courante et sont déjà consommés par Buildings/Research/Production/Defense/Fleet, les canaux SSE tutorial-quest-pending/complete invalident le cache en temps réel, et getActionLink fournit des deep-links contextuels par type de condition (y compris fleet/send pré-rempli coordonnées+mission) — une séquence 'construire la mine → chantier → premier vaisseau de minage → première mission du centre de missions' est réalisable sans nouveau condition_type.
  PREUVE : /opt/exilium/apps/web/src/hooks/useTutorialHighlight.ts + /opt/exilium/apps/web/src/hooks/useNotifications.ts:212-217 + OnboardingButton.tsx getActionLink (navigate fleet/send?galaxy=&system=&position=8&mission=mine&pveMissionId=)
- FAIT : Le pont tutoriel→PvE existe déjà et préfigure exactement les 'missions d'introduction' : à la complétion de quest_15, le service génère une mission de minage PvE dédiée en position 8 du système du joueur et stocke son id en metadata pour le deep-link — le pattern 'le tutoriel fabrique une vraie mission du jeu' est codé et fonctionne.
  PREUVE : tutorial.service.ts:411-428 (pveService.generateDiscoveredMission + metadata.tutorialMiningMissionId)
- FAIT : Trois comptes (Seb57, Daium, DebugBot) n'ont aucune ligne tutorial_progress : inscrits mais jamais arrivés jusqu'au premier chargement du jeu — en excluant les comptes de test (Testadmin, DebugBot), 10 comptes réels sont aujourd'hui bloqués avant le chapitre 4 côté joueurs actifs non-finishers (5 à quest_1, puis quest_4, quest_5, quest_8, quest_16, plus 2 jamais entrés).
  PREUVE : SELECT u.username ... LEFT JOIN tutorial_progress WHERE is_complete IS DISTINCT FROM true (12 lignes dont Testadmin et DebugBot)
FICHIERS CLÉS : /opt/exilium/apps/api/src/modules/tutorial/tutorial.service.ts, /opt/exilium/apps/api/src/modules/tutorial/tutorial.router.ts, /opt/exilium/packages/game-engine/src/sidebar-visibility.ts, /opt/exilium/apps/web/src/components/layout/Sidebar.tsx, /opt/exilium/apps/web/src/components/layout/BottomTabBar.tsx, /opt/exilium/apps/web/src/components/layout/topbar/OnboardingButton.tsx, /opt/exilium/apps/web/src/hooks/useTutorialHighlight.ts, /opt/exilium/apps/web/src/hooks/useNotifications.ts, /opt/exilium/apps/api/src/workers/build-completion.worker.ts, /opt/exilium/apps/api/src/workers/fleet.worker.ts, /opt/exilium/apps/api/src/modules/flagship/flagship.router.ts
QUESTIONS : Carte neuve au lancement : les 10 comptes scellés hors chapitre 4 deviennent-ils sans objet (reset complet des tutorial_progress) ou faut-il un geste de compensation pour les vétérans qui retentent l'aventure ? | Les missions d'introduction du centre de missions doivent-elles être concurrentes dès la v1 (ce qui impose de remplacer le mono-pointeur current_quest_id par une table d'instances de mission par joueur) ou une intro linéaire de ~5 étapes suffit-elle, la concurrence n'arrivant qu'avec le flux normal du centre de missions ? | Garder le clic manuel 'Suivant' (rythme narratif + claim de récompense, cohérent avec un centre de missions) ou auto-compléter avec toast pendant les 5 premières minutes ? Les données plaident pour l'auto-complétion au début : 2 des 9 comptes en cours sont figés uniquement parce qu'ils n'ont jamais cliqué. | Le dévoilement progressif de la sidebar est un bon principe mais il est piloté par les chapitres du tutoriel et absent du mobile : faut-il le re-piloter par jalons de gameplay (premier chantier construit, première flotte envoyée) et l'unifier desktop/mobile, sachant que le mobile actuel montre tout ? | Définir la cible exacte des '5 minutes' : jusqu'à la première mission de minage lancée (le pont PvE existe déjà), ou jusqu'au premier contact scénarisé avec un système des Premiers (nécessite que la génération des systèmes tenus par les Premiers existe au lancement) ? | La fiction 'arbre de recherche = mémoire retrouvée via les silos' implique-t-elle que la Recherche soit visible mais verrouillée narrativement dès la première connexion (teasing) plutôt que masquée comme aujourd'hui ?